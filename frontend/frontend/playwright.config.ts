const fs = require("fs"); const { defineConfig, devices } = require("@playwright/test"); const baseURL = process.env.PLAYWRIGHT_ADMIN_BASE_URL ?? "http://localhost:3005"; const storageState = process.env.PLAYWRIGHT_STORAGE_STATE ?? "playwright/.auth/adminAuthState.json"; const storageStatePath = fs.existsSync(storageState) ? storageState : undefined; module.exports = defineConfig({ testDir: "./tests", timeout: 30000, expect: { timeout: 5000 }, fullyParallel: false, retries: process.env.CI ? 2 : 0, reporter: [["list"], ["html", { open: "never" }]], use: { baseURL, trace: "on-first-retry", screenshot: "only-on-failure", video: "retain-on-failure", actionTimeout: 10000, ignoreHTTPSErrors: true }, projects: [{ name: "setup", testMatch: /.*\.setup\.playwright\.spec\.ts/ }, { name: "chromium", dependencies: ["setup"], testIgnore: /.*\.setup\.playwright\.spec\.ts/, use: { ...devices["Desktop Chrome"], ...(storageStatePath ? { storageState: storageStatePath } : {}) } }] });
